/**
 * Worker scaffold (Phase 4).
 *
 * We deploy this to Railway and attach BullMQ processors:
 * - generate-page
 * - export-pdf (optional)
 *
 * For Phase 1/2 we keep this minimal so the monorepo structure is complete.
 */

console.log('[worker] scaffold ready (Phase 4 will add queues/processors).');


